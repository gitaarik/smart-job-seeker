# Commits that changed formatting only, and would otherwise make `git blame`
# attribute every line to a whitespace pass.
#
# Local git does not read this automatically. Enable it once per clone:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub applies it to its blame view without any setup.

# style: format the tree with prettier
# 846 files. Settled three contradictory formatter configs — deno.json's fmt
# block, a scaffold .prettierrc, and an unrunnable `npm run format` — onto
# prettier, which unlike deno can actually format the 227 .svelte files.
e7e2b260dc114a0b198d74a653bf93220a62fb15
