<!--
  Public-profile root page is disabled — `+page.server.ts` throws 404
  before this template ever renders. The original template referenced
  `data.profile.*` heavily; that's preserved in git history. The
  `./portfolio/components/` subtree is also retained for re-enabling
  the feature later. This file is intentionally minimal so it stays
  valid as the data contract evolves.
-->
