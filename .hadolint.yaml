# DL3008 wants every apt package pinned to a version. Debian's and Ubuntu's
# archives keep only the current version of each package, so a pin fails the
# build as soon as a security update replaces it: that trades a patched
# package for a broken build. What pins the distro here is the base image tag.
ignored:
  - DL3008
