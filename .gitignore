node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Local tool directories
.serena/

# Env
.env
.env.keys
.env.*
!.env.example
!.env.test
!.env.project
!.env.vault
# Using dotenvx now, and `.env.production` is encrypted,
# and should be committed to the repo.
!.env.production

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

/generated/prisma

# Chrome profiles for job scraping
chrome-profiles/

# Full database dumps (large, use db-dumps/smart.sql for git)
db-dumps/full.sql

# User uploads (keep .gitkeep only)
uploads/*
!uploads/.gitkeep
