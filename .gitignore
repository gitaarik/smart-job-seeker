node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Local tool directories
.serena/

# Env
.env
.env.keys
.env.*
!.env.example
!.env.test
!.env.project
!.env.vault
# Using dotenvx now, and `.env.production` is encrypted,
# and should be committed to the repo.
!.env.production

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Chrome profiles for job scraping
chrome-profiles/

# Database dumps — may contain REAL user data (PII, hashes, credentials).
# NEVER commit these; they were purged from history once already.
# Ship a synthetic seed for public dev instead of a real dump.
db-dumps/*
!db-dumps/.gitkeep

# User uploads (keep .gitkeep only)
uploads/*
!uploads/.gitkeep
